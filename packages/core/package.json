{
  "name": "@colorsense/core",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "공통 날씨 모델 · 기상청 매핑. 웹과 BFF가 같은 구현을 쓰기 위한 패키지.",
  "exports": { ".": "./dist/index.js" },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
